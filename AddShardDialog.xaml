<Window x:Class="URUManager.AddShardDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource DialogTitle_Add}" Height="300" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#FF3C3C3C" Foreground="White">
    <Grid Margin="16">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#FF555555"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#FF666666"/>
                <Setter Property="CaretBrush" Value="White"/>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="White"/>
            </Style>
            <Style x:Key="DeleteButtonStyle" TargetType="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#CC993333"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#CC551111"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Background" Value="#AA662222"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource LabelName}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" x:Name="NameBox"/>

        <!-- Pfad -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource LabelPath}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PathBox"/>
        <Button Grid.Row="2" Grid.Column="2" Content="..." Width="32" Margin="4,0,0,0"
                Click="BrowseButton_Click"/>

        <!-- Arguments -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource LabelArguments}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" x:Name="ArgumentsBox"/>

        <!-- Internal -->
        <CheckBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" x:Name="InternalCheck"
                  Content="{DynamicResource LabelInternal}" VerticalAlignment="Center"/>

        <!-- Delete TOS -->
        <CheckBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2" x:Name="DeleteTosCheck"
                  Content="{DynamicResource LabelDeleteTos}" VerticalAlignment="Center"/>

        <!-- Buttons -->
        <Grid Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,8,0,0">
            <Button x:Name="DeleteButton" Content="{DynamicResource DeleteShard}" Width="80"
                    HorizontalAlignment="Left" Visibility="Collapsed" Padding="8,4"
                    Style="{StaticResource DeleteButtonStyle}"
                    Click="DeleteButton_Click"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="80" Margin="0,0,8,0"
                        Click="OkButton_Click" IsDefault="True"/>
                <Button Content="{DynamicResource Cancel}" Width="80"
                        Click="CancelButton_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
